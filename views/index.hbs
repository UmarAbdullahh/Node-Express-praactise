<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>
</head>
<body>
    <table>
        <thead>
            <tr>Id</tr>
            <tr>Name</tr>
            <tr>Location</tr>
            <tr>Writer</tr>
        </thead>
        <tbody>
            {{#each book}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.name}}</td>
                <td>{{this.location}}</td>
                <td>{{this.writer}}</td>
                <td><form action="/edit/{{this.id}}" method="get">
                    <button type="submit">Edit</button></form></td>
                <td> <form action="/delete/{{this.id}}" method="post">
                    <button type="submit">Delete</button></form></td>
            </tr>
           {{/each}}
        </tbody>
    </table>
        <button type="button"><a href='/createBook'>Add Book</a></button>

</body>
</html>